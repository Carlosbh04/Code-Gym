import { ArrowRight, Play } from 'lucide-react';
import { Link } from 'react-router-dom';
import type { ExerciseSession } from '@/types/exercise';

export function ContinueCard({ session, conceptName, isLoading, practicedConcepts }: { session: ExerciseSession | null; conceptName: string | null; isLoading: boolean; practicedConcepts: number }) {
  const href = session === null ? '/tech/javascript' : `/practice/${session.id}`;
  return <article className="relative overflow-hidden rounded-2xl border border-border bg-card p-5 sm:p-6 lg:p-7"><div aria-hidden="true" className="pointer-events-none absolute -right-20 -top-24 size-56 rounded-full bg-primary/10 blur-3xl" /><div className="relative flex h-full flex-col"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-muted-foreground">Continúa donde lo dejaste</p><div className="mt-5 flex items-start gap-4"><span className="flex size-12 shrink-0 items-center justify-center rounded-xl border border-primary/30 bg-primary/10 text-primary"><Play className="size-5" aria-hidden="true" /></span><div className="min-w-0"><h2 className="text-xl font-bold tracking-tight text-foreground sm:text-2xl">{isLoading ? 'Preparando tu próxima práctica…' : session?.title ?? 'Empieza tu próxima sesión'}</h2><p className="mt-2 text-sm leading-relaxed text-muted-foreground">{session === null ? 'Elige un ejercicio real para comenzar a construir tu progreso.' : `${conceptName ?? 'JavaScript'} · ${session.steps.length} ejercicios`}</p></div></div><div className="mt-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><p className="text-sm text-muted-foreground">{practicedConcepts === 0 ? 'Sin actividad registrada todavía' : `${practicedConcepts} conceptos con actividad`}</p><Link to={href} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground transition-colors duration-fast ease-standard hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background">{session === null ? 'Explorar JavaScript' : 'Continuar'} <ArrowRight className="size-4" aria-hidden="true" /></Link></div></div></article>;
}
